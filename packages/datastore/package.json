{
  "name": "@pooky/datastore",
  "version": "0.1.0",
  "description": "data store layer for pooky api",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "babel --root-mode upward --no-comments -d dist src/",
    "coverage": "jest --coverage",
    "test": "jest",
    "test:watch": "jest --watch",
    "postinstall": "yarn run build",
    "lint": "eslint --ext js src/",
    "lint:fix": "eslint --fix --ext js src/"
  },
  "dependencies": {
    "@pooky/structures": "^0.1.0",
    "ioredis": "^4.14.1",
    "prop-types": "^15.7.2",
    "redis": "^2.8.0",
    "uuid": "^3.3.2"
  }
}
